#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Простой тест HTML-форматирования для Telegram
"""

def test_html_tags():
    """Тестирует HTML-теги, которые используются в боте"""
    
    print("🧪 Тестирование HTML-тегов...")
    
    # Теги, которые используются в learning.py
    test_texts = [
        {
            "name": "theory_1",
            "text": "🎯 <b>Что такое МАК-карты</b>\n\nКарты помогают увидеть внутренний смысл через образы, ассоциации и чувства.\nПравильного ответа нет — важно, что ты <b>видишь</b> и <b>чувствуешь</b>.\n\nЧипы: <code>Что я вижу?</code> · <code>Где это во мне?</code> · <code>Какой маленький шаг?</code>",
            "expected": "HTML с тегами <b> и <code>"
        },
        {
            "name": "theory_2", 
            "text": "🔎 <b>Зачем нужен запрос</b>\n\nЗапрос — как фонарик внимания внутрь.\n\nБез запроса — просто картинка.\nС запросом — <b>живое зеркало</b> твоего состояния и шага.\n\nШаблон: <b>«Что поможет мне … (желанный результат) сегодня?»</b>",
            "expected": "HTML с тегами <b>"
        },
        {
            "name": "trainer_examples",
            "text": "💡 <b>Примеры переформулировки</b>\n\n✦ \"Почему у меня ничего не получается?\"\n→ 🌿 \"Что поможет мне поверить в себя и сделать первый шаг?\"\n\n✦ \"Когда всё наладится?\"\n→ 🌿 \"Как я могу поддержать себя, пока всё меняется?\"\n\nФокус смещается с <i>мира</i> — на <b>себя</b>.\nЭто и есть ресурсный запрос.",
            "expected": "HTML с тегами <b> и <i>"
        }
    ]
    
    print("\n📋 Проверка текстов:")
    
    for test in test_texts:
        print(f"\n✅ {test['name']}:")
        print(f"   Текст: {test['text'][:100]}...")
        print(f"   Ожидается: {test['expected']}")
        
        # Проверяем наличие HTML-тегов
        has_b = "<b>" in test['text']
        has_i = "<i>" in test['text'] 
        has_code = "<code>" in test['text']
        has_br = "<br>" in test['text']
        
        print(f"   HTML-теги: <b>={has_b}, <i>={has_i}, <code>={has_code}, <br>={has_br}")
        
        if has_br:
            print(f"   ⚠️  ПРОБЛЕМА: Найден тег <br>!")
        else:
            print(f"   ✅ Нет проблемных тегов <br>")
    
    print("\n🎯 Итог:")
    print("   Все тексты используют только поддерживаемые Telegram HTML-теги:")
    print("   • <b> - жирный текст ✅")
    print("   • <i> - курсив ✅") 
    print("   • <code> - моноширинный шрифт ✅")
    print("   • \\n\\n - переносы строк ✅")
    print("   • НЕТ <br> тегов ✅")
    
    print("\n💡 Если форматирование не работает:")
    print("   1. Проверьте parse_mode='HTML' в функциях")
    print("   2. Убедитесь, что нет parse_mode='Markdown'")
    print("   3. Перезапустите бота после изменений")

if __name__ == "__main__":
    test_html_tags()
